services:
  db:
    image: postgres:15-alpine
    container_name: auth-service-db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: auth_db
    ports:
      - '5435:5432'